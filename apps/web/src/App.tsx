// Placeholder shell. The real workbench layout (top bar, three-column
// grid, status bar) lands in later tasks once the store is wired in.
export function App() {
  return (
    <>
      <header>Jev.UI</header>
      <main />
    </>
  );
}
